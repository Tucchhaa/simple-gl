#include "light.h"

#include "../scene.h"
#include "../../managers/engine.h"

namespace SimpleGL {
// Light::Light(const std::string &name): Component(name) {
//     Engine::get()->scene()->addLight(std::static_pointer_cast<Light>(shared_from_this()));
// }

}
